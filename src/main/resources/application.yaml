
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/patrimonio
    username: postgres
    password: Shukako1234

  jpa:
    hibernate:
      ddl-auto: validate

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

server:
  port: 8080

